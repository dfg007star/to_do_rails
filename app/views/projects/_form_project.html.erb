<%= form_for @project, remote: true do |f| %>
<div class="d-flex align-items-center justify-content-center mb-2">
<span class="input-group-text m-0 rounded-0 border-right-0 rounded-left bg-primary text-light" id="basic-addon1">✚</span>
<div class="d-flex">
<%= f.text_field :name, class: "form-control rounded-0"%>
</div>
<div class="d-flex">
<%= f.submit "Add Project", class: "btn btn-primary rounded-0 rounded-right pl-2 pr-2" %>
</div></div>
<% end %>
